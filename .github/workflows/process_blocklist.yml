name: Process Blocklist

on:
  push:
    branches:
      - main
    paths:
      - 'blocklist.txt'
      - 'process_blocklist.py'
  pull_request:
    branches:
      - main
    paths:
      - 'blocklist.txt'
      - 'process_blocklist.py'

permissions:
  contents: write

jobs:
  validate-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          check-latest: true

      - name: Validate and Process Blocklist
        id: validate
        run: python process_blocklist.py blocklist.txt --fix --stats

      - name: Commit and Push changes
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          if [[ -n $(git status -s blocklist.txt) ]]; then
            git add blocklist.txt
            git commit -m "Auto-update: Metadata, sort, and auto-fixes [skip ci]"
            git pull --rebase origin main
            git push
          else
            echo "No changes needed."
          fi